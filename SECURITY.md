# Security policy

司南当前是邀请制 Beta。请不要在公开 issue、PR 或聊天中提交密码、数据库连接串、API token、邮箱验证码或用户数据。

## 报告漏洞

请通过 GitHub Security Advisories 或直接联系仓库所有者报告安全问题，并提供：

- 受影响的 URL、接口或文件。
- 可复现步骤和影响范围。
- 是否可能暴露用户、认证或生产数据。
- 建议的修复方向（如果有）。

我们会先确认影响，再决定修复、轮换密钥和通知范围。不要为了证明问题读取、修改或导出真实用户数据。

## 密钥处理

- 生产密钥只保存在阿里云 Secret Manager/ECS 运行环境、EAS 或受保护的 GitHub Environment。
- 不把密钥写入源码、镜像、日志、截图或 issue。
- 怀疑泄露时立即撤销并轮换，不依赖删除 Git 历史。
- 生产数据库不开放公网，应用使用最小权限账号。
