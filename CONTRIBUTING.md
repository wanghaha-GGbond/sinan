# Contributing to Sinan

## 分支

- `main` 是唯一长期分支，禁止直接 push。
- 每个任务从最新 `origin/main` 创建短期分支。
- 一个 PR 只解决一个主题；合并后删除分支。
- 不从已关闭 PR、旧 release 或归档分支继续开发。

## Web 与 iOS 边界

- Web 页面、API、数据库和迁移放在 `apps/web`。
- iOS 客户端放在 `apps/ios`。
- 两端确实共用的类型和逻辑才放在 `packages/shared`。
- 不在 iOS 中复制 Web API 契约；接口变化要先更新共享类型或契约测试。

## Pull Request

PR 描述必须包含：

1. 目标和用户行为变化。
2. 修改范围和是否有数据库迁移。
3. 真实数据、演示数据和 feature flag 状态。
4. 测试命令及结果。
5. 风险、回滚方式和需要人工完成的配置。

PR 必须通过对应 CI、数据库检查（如适用）和 Preview 检查，并获得另一位成员批准。不要绕过分支保护或把失败的部署检查标记为成功。

## 生产规则

- 不用 mock 数据掩盖 API、数据库或邮件失败。
- 不提交固定验证码、假身份、假成功状态或生产密钥。
- 不执行向下数据库迁移；使用向前兼容迁移和修复版本。
- 破坏性 API 变化从 `/api/v2` 开始，现有 `/api/*` 保持兼容。
